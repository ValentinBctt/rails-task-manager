<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
       <%= check_box_tag "task[#{task.id}]", task.completed %> <%= link_to task.title, task_path(task)%>
    </li>
  <% end %>
</ul>

<%= link_to "New Task", "tasks/new" %>
